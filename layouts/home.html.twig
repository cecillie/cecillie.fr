{% extends ['page.html.twig', '_default/page.html.twig'] %}

      {%- block content ~%}
      <p class="font-extrabold text-6xl my-4">{% trans %}Hello{% endtrans %},</p>
      <h1 class="font-bold text-xl">{{ page.title }}</h1>
      <p class="font-bold text-xl">{{ page.description }}</p>
      <div class="leading-normal text-xl mb-4 my-4">
        {{- page.content ~}}
      </div>
      {%- endblock content ~%}

      {% block footer %}{% endblock footer %}