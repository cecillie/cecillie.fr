{% extends '_default/page.html.twig' %}

{%- block content ~%}
      <p class="font-extrabold text-5xl my-4">Hey there,</p>
      <h1 class="font-bold">{{ page.title }}</h1>
      <p class="font-bold">{{ page.description }}</p>
      <div class="leading-normal mb-4 my-4">
        {{- page.content ~}}
      </div>
{%- endblock content ~%}
