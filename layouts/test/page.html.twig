{% extends ['page.html.twig', '_default/page.html.twig'] %}

      {%- block head ~%}
      {{ parent() }}
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.css">
      {%- endblock ~%}

      {%- block content ~%}
      <article class="mx-auto max-w-(--breakpoint-md) prose md:prose-lg lg:prose-xl prose-img:mx-auto prose-figcaption:text-center prose-img:mb-1 lg:prose-img:mb-1">
        <header>
          <h1>{{ page.title }}</h1>
          {%- set readtime = readtime(page.content) ~%}
          <time>{{ page.date|format_date('long') }}</time>
        </header>
        <div class="swiper mySwiper">
          <div class="swiper-wrapper">
            {% for image in page.images %}
            <div class="swiper-slide">
              <img src="{{ asset(image.file) }}" alt="{{ image.alt }}" class="w-full h-auto object-cover">
            </div>
            {% endfor %}
          </div>
          <div class="swiper-pagination"></div>
        </div>
        {{~ page.content ~}}
      </article>
      {%- endblock content ~%}

    {%- block scripts ~%}
      {{ parent() }}
      <script src="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.js"></script>
      <script>{% apply minify_js ~%}
        var swiper = new Swiper('.mySwiper', {
          pagination: {
            el: '.swiper-pagination',
          },
        });
      {% endapply ~%}</script>
    {%- endblock ~%}

    {%- block noise %}{% endblock ~%}