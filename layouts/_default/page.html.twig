{% set title = site.title ~ ' &middot; ' ~ site.baseline %}
<!doctype html>
<html lang="{{ site.language }}">
<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    <meta name="title" content="{{ title }}">
    <meta name="description" content="{{ site.description }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ url(page, {canonical: true}) }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ site.description }}">
    <meta property="og:image" content="{{ url('apple-touch-icon.png', {canonical: true}) }}">
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="{{ url(page, {canonical: true}) }}">
    <meta property="twitter:title" content="{{ title }}">
    <meta property="twitter:description" content="{{ site.description }}">
    <meta property="twitter:image" content="{{ url('apple-touch-icon.png', {canonical: true}) }}">
    <link rel="apple-touch-icon" href="{{ url('apple-touch-icon.png') }}">
    <link rel="shortcut icon" href="{{ url('favicon.ico') }}">
    {% filter minifyCSS %}
    <style type="text/css">
        html {
          -webkit-font-smoothing: antialiased;
          -webkit-text-stroke-width: 0;
          -moz-osx-font-smoothing: grayscale;
          -moz-hyphens: auto;
          -webkit-hyphens: auto;
          hyphens: auto;
          box-sizing: border-box;
          background: #fff;
          position: relative;
        }
        body {
          display: flex;
          align-items: center;
          justify-content: center;
          z-index: 0;
          -webkit-animation: backgroundscale 1s ease-out forwards;
          animation: backgroundscale 1s ease-out forwards;
          background: #fff;
        }
    </style>
    {% endfilter %}
    <link rel="preload" href="https://fonts.googleapis.com/css?family=Wire+One" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <link rel="preload" href="{{ url('css/normalize.css') }}" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <link rel="preload" href="{{ url('css/styles.css') }}" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Wire+One"></noscript>
    <noscript><link rel="stylesheet" href="{{ url('css/normalize.css') }}"></noscript>
    <noscript><link rel="stylesheet" href="{{ url('css/styles.css') }}"></noscript>
{% include 'partials/alternates.html.twig' %}
</head>
<body>
    <article>
        <div id="logo">
            <div class="logo">
                <img src="{{ url('img/logo.svg') }}" alt="Logo">
            </div>
        </div>
{% block header %}
        <header>
{% block content %}
            {{ page.content }}
{% endblock content %}
        </header>
{% endblock header %}
{% block footer %}
        <footer>
            <div>
                <h2>
{% for link, url in site.links %}
                    <a href="{{ url }}" target="_blank" rel="noopener">{{ link }}</a>
{% endfor %}
                </h2>
            </div>
        </footer>
{% endblock footer %}
    </article>
{% include 'partials/googleanalytics.js.twig' %}
</body>
</html>
